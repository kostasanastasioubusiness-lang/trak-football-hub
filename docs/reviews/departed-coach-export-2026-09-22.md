# Departure-safe roster exports — local review proposal

## Problem and intended behavior

U8 requires a removed/transferred coach to lose former-academy access. On the isolated test project, the already-removed synthetic coach freshly authenticated with organization_id NULL: ordinary table reads returned zero former roster/assessment rows, while export_my_account returned both former roster rows and both retained assessments. The privileged export checked author ID but not current roster ownership.

This proposal keeps current authorized coach exports and the caller's own profile available, but excludes former-academy roster, assessment, private-note and award records. It leaves retained records in the academy. Other role branches and the observation-scope switch are unchanged.

## Implementation

One forward migration, generated by the installed Supabase CLI as 20260922091647_restrict_departed_coach_exports.sql, replaces export_my_account. Historical migrations remain unchanged. The four roster-linked collections retain their author-ID condition and also use squad_player_is_mine, the existing current-access predicate. Notes reach that predicate through their assessment. Existing Auth requirement, function security mode and execution grants remain intact.

The SQL test now creates actual academy membership before inserting roster rows, calls remove_coach_from_org as its administrator, and exports as the removed coach. One original roster row is released: removal leaves that status unchanged, so academy ownership matters independently of the departed-status check. A transfer into another academy and new authorized records then prove that exclusion does not disable valid exports. The fixture uses main's join_organization contract when the staff invitation RPC is absent; in the staff-admission composition it issues and accepts a real staff invitation. No privileged membership update bypasses the guard.

## Evidence

Artifacts: artifacts/2026-09-22-departed-coach-export/ at the workspace root.

- Original source plus regression fails: removed coach export includes former academy roster.
- Corrected departure and transfer suite passes, preserving existing player/privacy/anonymous-denial controls and current-coach positive controls.
- Independently removing each new roster, assessment, note or award filter causes its expected departure assertion to fail. All four mutations caught; original candidate restored afterwards.
- All20canonical commands pass, including the nativePostgreSQL17 academy-upgrade replay with account_export.sql and10browser checks. Results are recorded in full-ci/summary.json. The native runner did not run the hosted security advisor.
- Isolated hosted correction is applied with its original migration version. The removed coach now exports no former records, while current authorized sets remain intact: artifacts/2026-09-21-test-project-setup/HOSTED-DEPARTURE-EXPORT-RESULTS.md. Production remains unchanged.
- Additional hosted removal/transfer proof uses one unchanged coach session throughout academy A, removal, and academy B: 43 checks/60 requests pass. Former roster, assessments, notes and awards stay inaccessible; new authorized exports work and retained academy records remain. Evidence: artifacts/2026-09-21-test-project-setup/HOSTED-TRANSFER-EXPORT-RESULTS.md.
- Staff-admission compatibility: the original join-code-only fixture fails on the combined tree, whose membership guard correctly requires an invitation. The corrected fixture passes all 20 standalone checks (670 source tests, nine existing skips, ten browser checks) and all 24 checks on combined candidate b55f9aa (920 source tests, nine skips, 21 pilot and 11 default browser checks). Evidence: artifacts/2026-09-22-concurrent-browser/pr108-compat-ci/summary.json and full-ci/summary.json. Initial sandbox failures were environmental: PostgreSQL shared memory and loopback listening were denied; the four affected commands passed with the required local permissions. No migration/function changes accompany this test update.

## Release boundaries

This is an application authorization correction, not a legal determination of portability/retention entitlements. Kostas/Tarek must review the self-service boundary and retained coach work-product treatment before deployment. Existing sessions/calendar exports retain their ordinary author-access behavior; this patch does not close all U8 paths or assign academy ownership to those tables. AI/media/browser caches and all-session revocation remain separate checks.

After review and explicit production deployment authorization: apply the forward migration to production, repeat the approved bounded probe there, and verify an authorized current-coach export as a positive control. Do not delete fixtures/history or rewrite migration history. If correction is needed after deployment, use another reviewed forward migration; do not restore the demonstrated disclosure as rollback.

Official function-security reference consulted: https://supabase.com/docs/guides/database/functions . Supabase changelog reviewed on September22; no relevant function API breaking change identified for this SQL-only replacement. Local/native tests are not a hosted security-advisor result.
